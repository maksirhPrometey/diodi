"""
Пакет налаштувань Django.

DJANGO_SETTINGS_MODULE:
  config.settings.develop   — локально (sqlite)
  config.settings.docker    — Docker / DigitalOcean Droplet
  config.settings.production — production без Docker
"""
